% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/time.R
\name{file_start_time}
\alias{file_start_time}
\title{Extract date time information from a file's name.}
\usage{
file_start_time(paths, posix_formats, tz, first_match = FALSE)
}
\arguments{
\item{paths}{Character vector of file paths.}

\item{posix_formats}{Character vector of POSIX format strings (see \link[base:strptime]{base::strptime}) describing the timestamp embedded in each file name (e.g. \code{'\%y\%m\%d_\%H\%M'} for \verb{230809_0600}). Supply multiple strings when recordings from different logger types are mixed in one directory.
See \link[base:strptime]{base::strptime} for format codes.}

\item{tz}{Time zone string passed to \link[base:as.POSIXlt]{base::as.POSIXct} (e.g. \code{'America/New_York'}). See \code{OlsonNames()} for valid values.}

\item{first_match}{Controls behaviour when multiple formats produce \emph{different} times for the same file. \code{FALSE} (default) returns \code{NA} with a warning; \code{TRUE} accepts the time from the first matching format with a message.}
}
\value{
A POSIXct vector the same length as \code{paths}.
}
\description{
Extract recording start date-times from file names.
}
\details{
Parses date-time information embedded in file names using one or more POSIX
format strings (see \link[base:strptime]{base::strptime} for format codes). The file extension
and \verb{_buzzdetect} suffix are stripped before matching, so the format string
should describe only the timestamp portion of the base name.

When a single format unambiguously matches, its parsed time is returned.
When multiple formats match the same file \emph{with the same result}, the
duplicate is silently ignored. When they produce \emph{different} times, the
behaviour depends on \code{first_match}:
\itemize{
\item \code{FALSE} (default): returns \code{NA} with a warning listing the conflicting formats.
\item \code{TRUE}: returns the time from the first matching format with a message.
}

If no format matches, \code{NA} is returned with a warning.
}
\examples{
# Single file, single format (YYMMDD_HHMM)
file_start_time(
  'soybean/9/230809_0000_buzzdetect.csv',
  posix_formats = '\%y\%m\%d_\%H\%M',
  tz = 'America/New_York'
)

# Multiple files in one call
paths <- c(
  'soybean/9/230809_0000_buzzdetect.csv',
  'chicory/1_104/250704_0000_buzzdetect.csv'
)
file_start_time(paths, posix_formats = '\%y\%m\%d_\%H\%M', tz = 'America/New_York')

# Two formats for two recorder types in the same experiment;
# first_match = FALSE returns NA (with a warning) if both match a file
file_start_time(
  paths,
  posix_formats = c('\%y\%m\%d_\%H\%M', '\%Y\%m\%d_\%H\%M\%S'),
  tz = 'America/New_York',
  first_match = FALSE
)
}
\seealso{
\link{read_results} which calls this internally when
\code{posix_formats} is supplied.
}
