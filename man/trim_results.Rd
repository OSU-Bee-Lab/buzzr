% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cleaning.R
\name{trim_results}
\alias{trim_results}
\title{Trim activation columns in a results data frame.}
\usage{
trim_results(results, activation_digits, neurons_keep = NULL)
}
\arguments{
\item{results}{A data frame or data.table with \code{activation_} columns.}

\item{activation_digits}{Integer. Number of decimal places to round activation values to.}

\item{neurons_keep}{Character vector of neuron names to retain. Names may include or omit
the \code{activation_} prefix (e.g. \code{"ins_buzz"} and \code{"activation_ins_buzz"} are equivalent).
All other activation columns are dropped. If \code{NULL} (default), all neurons are kept.}
}
\value{
A data.table with rounded (and optionally filtered) activation columns.
}
\description{
Rounds all \code{activation_} columns to the specified number of decimal places.
Optionally retains only the neurons named in \code{neurons_keep}, dropping all others.
This is useful for reducing file size before archiving or sharing results.
The original data frame is not modified.
}
\examples{
path <- system.file(
  'extdata/five_flowers/soybean/9/230809_0000_buzzdetect.csv',
  package = 'buzzr'
)
results <- read_results(path)

# Round all activation columns to 2 decimal places
trim_results(results, activation_digits = 2)

# Keep only the ins_buzz neuron and round to 1 decimal place
trim_results(results, activation_digits = 1, neurons_keep = 'ins_buzz')
}
\seealso{
\link{trim_directory} to apply this to an entire folder of files.
}
