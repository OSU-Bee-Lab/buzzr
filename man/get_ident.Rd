% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/files.R
\name{get_ident}
\alias{get_ident}
\title{Extract the identifier (ident) from a buzzdetect result file path.}
\usage{
get_ident(path_in, dir_in = "")
}
\arguments{
\item{path_in}{Character vector of file paths.}

\item{dir_in}{The root results directory to strip from the front of each
path. Defaults to \code{''} (returns the full path minus extension and tag).}
}
\value{
A character vector of the same length as \code{path_in}.
}
\description{
Strips the results directory prefix, the \verb{_buzzdetect} tag, and the file
extension, leaving a clean relative path that uniquely identifies the
recording. This ident can be used to locate corresponding audio files,
annotation files, or metadata.
}
\details{
For example, given \code{dir_in = '/data/results'} and
\code{path_in = '/data/results/soybean/9/230809_0000_buzzdetect.csv'}, the
returned ident is \code{'soybean/9/230809_0000'}.
}
\examples{
dir  <- system.file('extdata/five_flowers', package = 'buzzr')
path <- system.file(
  'extdata/five_flowers/soybean/9/230809_0000_buzzdetect.csv',
  package = 'buzzr'
)

# Returns 'soybean/9/230809_0000'
get_ident(path, dir)

# Works on a vector of paths
all_paths <- list.files(dir, pattern = '_buzzdetect', recursive = TRUE,
                        full.names = TRUE)
get_ident(all_paths, dir)
}
\seealso{
\link{read_directory} and \link{bin_directory} which can add
the ident as a column via \code{return_ident = TRUE}.
}
