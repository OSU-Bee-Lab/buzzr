# Get the time of day as a 0-1 value (left-inclusive) where 0 represents midnight

Get the time of day as a 0-1 value (left-inclusive) where 0 represents
midnight

## Usage

``` r
time_of_day(times, time_format = "proportion")
```

## Arguments

- times:

  A vector of POSIX times.
